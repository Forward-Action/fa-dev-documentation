{
  "license": "MIT",
  "name": "fa-dev-documentation",
  "version": "1.0.0",
  "description": "Forward Action Documentation",
  "devDependencies": {
    "honkit": "^6.1.4"
  },
  "scripts": {
    "honkit": "honkit build",
    "clean": "node -e \"require('fs').rmdirSync('docs/', { recursive: true }); process.exit(0);\"",
    "rename": "node -e \"require('fs').rename('_book', 'docs', function(err) { if (err) console.log(err); console.log('File successfully renamed!') })\"",
    "copy_cname": "node -e \"require('fs').copyFile('CNAME', 'docs/CNAME', function(err) { if (err) console.log(err); console.log('File successfully copied!') })\"",
    "start": "yarn honkit && yarn rename && yarn copy_cname",
    "build": "yarn honkit && yarn clean && yarn rename && yarn copy_cname",
    "serve": "honkit serve"
  }
}
